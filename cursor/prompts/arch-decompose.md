Deep decomposition plan for <PATH>. Confirm the hotspot with the detection pyramid (size, churn, fan-in, cycles, responsibilities). Pick a pattern from the playbook (Extract Module / Extract Port / Strangler Fig / Branch by Abstraction / Replace Temporal Coupling / Event-Carried State / Saga / ACL / Composition / Aggregate) via the decision tree — production-running code prefers Strangler Fig or Branch by Abstraction. Output: evidence table, chosen pattern + why, target architecture as Mermaid, PR sequence (≤5 PRs), rollout safety (flags, canary, parity), sunset date, risks. End with three options: proceed / defer / patch-in-place. No code writes.
