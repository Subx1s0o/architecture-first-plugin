Review ONE file for refactor: <file-path>. Read-only, returns a response, no files written. Compute: LoC, fan-in via grep, 90-day churn, responsibility clusters (verb-object groups). Find L1-L2 cleanup (debug logs, commented code > 30d, unused locals, dupe methods). Pick 1-3 concrete suggestions: each with Name (matched to decomposition-playbook pattern), Why (one sentence), First move (specific action — extract method X into file Y), Cost (LoC + risk). Output ends with two next-step options: light-touch via /arch-clean + /arch-clean-approve, or full decomposition via /arch-decompose.
