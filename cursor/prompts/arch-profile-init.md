Detect my project's stack (NestJS/Next/Express/Spring/Laravel/Django/FastAPI/Rails/Go/Rust/generic) by reading package.json, pyproject.toml, go.mod, etc. Generate .arch-profile.yaml at the project root with: layers (canonical names for this stack), modules (top-level dirs under src/), allowed-module-edges (empty, I'll fill), hot-modules (empty), thresholds (stack defaults), commands.test/build/lint. Refuse to overwrite if file exists.
