Execute cleanup batch <BATCH-ID> from the manifest. Refuse if the batch contains L3/L4. List every file/line to delete with evidence, ask "yes" to confirm. On yes: apply L1 edits / `git rm` L2 orphans / edit manifests + regenerate lockfile for L2 deps. Then run the project's test and build commands (from .arch-profile.yaml `commands:`). If anything fails — `git restore` and report the failure. Append the manifest with Status: executed, commit SHA, test/build result.
