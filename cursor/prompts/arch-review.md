Review the current `git diff` (staged + unstaged) for architectural red flags. For each changed source file: identify module and layer using .arch-profile.yaml or stack defaults. Flag: imports from another module's internals instead of its barrel, new event emissions without a subscriber grep-found, new queue jobs without a processor, new external calls duplicating existing ones, files over the stack's LoC threshold, new cycles. Output: table Severity | Location | Finding | Suggested seam. No edits.
