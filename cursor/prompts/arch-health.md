Repo-wide health dashboard. For each top-level module (typically src/<module>/) compute: total LoC (`wc -l`), 90-day churn via git log, cycles via madge/pydeps if available, worst single file. Classify 🟢 green (all within warn bounds) / 🟡 yellow (one past warn) / 🔴 red (two+ past warn, or any past xl) using .arch-profile.yaml thresholds or stack defaults. Write docs/arch-health.md with: per-module table (Module | LoC | Files | Churn 90d | Cycles | Worst file | Status), headline summary (N green / M yellow / K red, top risk), 3 recommended next moves. No source edits, no sub-agent. Suggest re-running monthly.
