Architectural review of a PR: <PR-number|URL|branch|sha-range>. Resolve to a diff via `gh pr diff <N>` (for PR number/URL — ask user to install gh if missing), `git diff $(git merge-base origin/HEAD <branch>)..<branch>` for branch, or `git diff <range>` for sha range. For each changed source file: identify module + layer via .arch-profile.yaml / stack profile. Flag cross-module barrel-bypass imports, orphan event emissions, new queue jobs without processors, duplicate external calls, files past LoC threshold, new cycles. Output a Severity | Location | Finding | Suggested seam table, plus summary (N files, K modules, tier XS/S/M/L, verdict approve / request-changes / needs-ADR). No edits. If user asks to post to PR, offer `gh pr comment <N> --body "..."` — don't run it automatically.
