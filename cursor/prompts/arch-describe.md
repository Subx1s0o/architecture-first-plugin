Produce a C4 architectural description of <SCOPE or whole service>. Stop at the smallest useful level — Context if external systems involved, Containers for cross-container work, Components for module-level, Code only for a single unit. Use Mermaid for levels 2-3. End with an "Intent vs. Reality" section if .arch-profile.yaml's allowed-module-edges disagree with what grep actually shows.
