Ownership + dependencies for a symbol/file: <arg>. Pure git + grep — no LLM reasoning beyond formatting. Resolve arg: symbol (rg -n for defs), file path (analyze whole file), file:line (narrow). Run: git log --follow --oneline -10 (last touches), git shortlog -sn --no-merges (top authors), rg -l '<symbol>' excluding defining file (callers, up to 10). Suggested reviewer = author of latest non-trivial touch AND heaviest net-line-contributor (if different people list both). Output: Defined in, Last touches, Top authors, Callers (with links), Suggested reviewer(s). No refactor recommendations.
