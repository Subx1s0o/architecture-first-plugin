Explain one file's role: <file-path>. Read-only, no sub-agent. Extract top-level exports + decorators/registrations. Grep callers (`rg -l '<exportName>'`) excluding self, show up to 10 with clickable links. Grep tests (`**/*.{spec,test}.*`). Note framework reach-outs (decorators / string-registered / convention-paths). Last 3 git log entries. Output markdown: Purpose (one paragraph), Exports, Called by (N places with links), Tests that pin it, Why it can't be trivially deleted (reachability mechanisms), Recent history. If truly orphan (0 callers, no framework reach) — say so plainly and suggest /arch-clean.
