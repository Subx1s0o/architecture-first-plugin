Run the cleaner over <SCOPE or whole repo>. For every finding classify as L1 (trivially safe: debug logs, old commented code, local unused vars), L2 (likely safe: orphan files, private exports with 0 importers, unused deps, obsolete flags, TODOs >180d), L3 (investigate: public exports, decorator-registered, string-dispatched, single-impl interfaces — raise level if reachable via DI/reflection/conventions), L4 (architect decides: dormant handlers/ports, legacy-compliance). Output a manifest with 4 tables (one per level) + proposed batches (A formatting, B orphan, C deps, D flags — one axis per batch). Every row has reproducible evidence (grep/git blame). No deletions.
