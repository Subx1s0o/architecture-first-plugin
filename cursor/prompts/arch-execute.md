Execute one PR from a decomposition plan. Argument forms: <DEC-N|N> [PR-M|M|next]. Default PR = first unexecuted.

Steps: (1) resolve DEC id and glob docs/decomposition/DEC-<padded>-*.md (stop if missing). (2) Parse the PR sequence and Execution log; pick the target PR. (3) Preflight: fail if git tree dirty; warn if on main/master and suggest `git checkout -b refactor/<DEC-id>-pr-<M>-<slug>`. (4) Show the PR scope and ask yes/no/show more. (5) On yes, implement the PR per the plan (no re-planning — DEC is the plan of record). (6) Run commands.test + commands.build from .arch-profile.yaml; on failure `git restore .` and report. (7) On green, commit: `refactor(<scope>): <PR title> [DEC-<id> PR-<M>/<total>]`. Do NOT push. (8) Append the DEC Execution log with date, commit sha, test/build status. (9) Report the next PR or mark DEC `Status: done`.
